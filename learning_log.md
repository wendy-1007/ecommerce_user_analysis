学习日志 - 2026年4月20日

============================================================

今日任务
--------
- 注册 Kaggle、GitHub、Tableau Public 账号
- 下载 Online Retail II 数据集
- 在 sqliteonline.com 中导入 CSV 文件
- 编写并运行每日销售汇总 SQL
- 导出结果 CSV 文件

============================================================

遇到的问题及解决方法
--------------------

1. 导入 CSV 文件
   操作：在 sqliteonline.com ，Import CSV，然后上传文件。
   结果：成功导入，表名为 online_retail_II，列名自动变为 c1 ~ c8，所有列类型均为 TEXT。

2. 列名变成 c1~c8，且无法直接计算
   原因：工具自动命名，且数字列被识别为文本。
   解决：查询时使用 c1 ~ c8 代替原列名，并用 CAST 转换数据类型（如 CAST(c4 AS INTEGER)）。

3. 日期列提取后包含了小时
   问题：使用 substr(c5, 1, 10) 时，因为日期长度不固定（如 1/10/11 10:26），导致结果变成 1/10/11 10。
   解决：改用 substr(c5, 1, instr(c5, ' ') - 1)，先找到空格位置，截取空格前的部分，得到纯日期。

4. 销售额出现很多小数位（如 32660.17000000002）
   原因：SQLite 的 REAL 浮点数计算精度问题。
   解决：用 ROUND(SUM(...), 2) 保留两位小数。

5. 发现 c4 列有负数
   观察：Quantity 列存在负数。
   原因：数据集中包含退货记录（负数量表示退货）。
   处理：当前销售汇总保留了负数，以便反映净销售额（真实收入）。后续 Python 阶段可单独分析退货行为。

============================================================

最终运行的 SQL 代码
--------------------

SELECT 
    substr(c5, 1, instr(c5, ' ') - 1) AS sale_date,   -- 提取日期（空格之前）
    COUNT(DISTINCT c1) AS order_count,                -- 订单数（去重）
    ROUND(SUM(CAST(c4 AS INTEGER) * CAST(c6 AS REAL)), 2) AS total_revenue  -- 净销售额，保留两位小数
FROM online_retail_II
WHERE c7 IS NOT NULL AND c7 != ''                    -- 过滤无客户ID的记录
GROUP BY substr(c5, 1, instr(c5, ' ') - 1)           -- 按日期分组
ORDER BY sale_date;

============================================================

今日产出
--------
- daily_sales.csv：每日销售汇总（含 sale_date, order_count, total_revenue）
- 本学习日志

============================================================

下一步计划
----------
- 下一个任务：编写第二个 SQL 查询（用户 RFM 表）
- 预计用时：2 小时
- 需要的数据：同一个数据集
