学习日志 - 2026年4月20日（更新版）

============================================================

今日任务
--------
- 注册 Kaggle、GitHub、Tableau Public 账号
- 下载 Online Retail II 数据集
- 在 sqliteonline.com 中导入 CSV 文件
- 编写并运行每日销售汇总 SQL
- 处理日期格式、空值、数据类型等问题
- 导出最终的 daily_sales.csv

============================================================

遇到的问题及解决方法
--------------------

1. 导入 CSV 后列名变成 c1~c8，数字列无法计算
   现象：导入后列名为 c1, c2, ... c8，所有类型都是 TEXT。
   原因：在线 SQL 工具自动命名列，且 CSV 所有字段默认识别为文本。
   解决：查询时使用 c1~c8 代替原列名，用 CAST(c4 AS INTEGER) 和 CAST(c6 AS REAL) 转换数据类型后再相乘。

2. 日期列提取时包含了小时
   现象：substr(c5, 1, 10) 得到的结果有时是 "1/10/11 10"（带小时）。
   原因：日期长度不固定（如 "1/10/11 10:26" 和 "2012/1/10 8:26"），固定截取10个字符会切到小时。
   解决：改用 substr(c5, 1, instr(c5, ' ') - 1)，先找到空格位置，截取空格前的所有字符，得到纯日期。

3. 销售额出现很多小数位（如 32660.17000000002）
   原因：SQLite 的 REAL 浮点数计算精度问题。
   解决：用 ROUND(SUM(...), 2) 保留两位小数。

4. 发现 c4 列有负数
   观察：Quantity 列存在负数。
   原因：数据集中包含退货记录（负数量表示退货）。
   处理：当前销售汇总保留了负数，以便反映净销售额（真实收入）。后续 Python 阶段可单独分析退货行为。

5. 运行 date(c5) 函数返回 NULL
   现象：SELECT date(c5) FROM ... 结果全是 NULL。
   原因：SQLite 的 date() 函数无法识别数据中的日期格式（例如 "1/10/11 10:26" 或 "2012/1/10 8:26"）。
   解决：放弃 date() 函数，使用 substr + instr 手动提取空格前的字符串作为日期。

6. 出现 sale_date 为空（NULL）的一行，且 order_count=1, total_revenue=0
   现象：结果表格中有一行 sale_date 为空，订单数为1，销售额为0。
   原因：某些记录的 InvoiceDate 列中没有空格（例如缺失或格式异常），导致 instr(c5,' ') = 0，substr(c5,1,-1) 返回空字符串。所有这样的行被分到同一组。
   解决：在 WHERE 子句中增加 AND instr(c5, ' ') > 0，排除这些无效行。

7. 日期中出现错误年份 2001（如 2001/10/11）
   原因：之前尝试手动构建 YYYY-MM-DD 格式时，解析逻辑错误，误将年份解析为 2001。
   解决：不再手动重构日期，直接使用原始提取的字符串（如 "1/4/11" 或 "2012/1/10"），排序问题留到 Python 阶段处理。

8. 不理解 INTEGER 和 REAL 的区别
   学习：INTEGER 是整数（无小数点），REAL 是实数（带小数点）。用 CAST 把文本转成数字才能做乘法求和。

9. 不理解 WHERE c7 IS NOT NULL AND c7 != '' 是否重复
   学习：NULL 表示“未知/缺失”，空字符串 '' 表示“已知但为空文本”，两者不同。数据中可能同时存在，所以都要排除。

============================================================

最终运行的 SQL 代码（正确版本）
--------------------------------

SELECT 
    substr(c5, 1, instr(c5, ' ') - 1) AS sale_date,
    COUNT(DISTINCT c1) AS order_count,
    ROUND(SUM(CAST(c4 AS INTEGER) * CAST(c6 AS REAL)), 2) AS total_revenue
FROM online_retail_II
WHERE c7 IS NOT NULL AND c7 != ''
  AND instr(c5, ' ') > 0
GROUP BY substr(c5, 1, instr(c5, ' ') - 1);

说明：
- 提取日期：取 c5 中第一个空格前的部分（兼容不同长度）
- 订单数：统计不重复的订单号
- 销售额：数量×单价，求和后保留两位小数
- 过滤：排除无客户ID、无有效日期的记录

============================================================

今日产出
--------
- daily_sales.csv：每日销售汇总（约 500+ 行，包含 sale_date, order_count, total_revenue）
- 本学习日志（记录了遇到的问题及解决过程）

============================================================

下一步计划
----------
- 任务：编写第二个 SQL 查询（用户 RFM 表）
- 或直接进入 Python 阶段，用 pandas 处理日期排序和深度分析
- 预计用时：2-3 小时
